# from django.apps import AppConfig

# class TanProjectConfig(AppConfig):
#     default_auto_field = 'django.db.models.BigAutoField'
#     name = 'tan_project'  # Ensure this matches your app folder name
